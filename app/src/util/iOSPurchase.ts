import {showMessage} from "../dialog/message";

export const processIOSPurchaseResponse = (_code: number) => {
    showMessage(window.siyuan.languages["_kernel"][29], 0, "error");
};

export const iOSPurchase = (_productType: string) => {
    showMessage(window.siyuan.languages["_kernel"][29], 0, "error");
};
